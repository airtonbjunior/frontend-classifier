/* Actually, I'm only returning the class (and the description) of the text. In the future, I'll return more informations */

export class ResponseClassifier {
    class: string;
    //classDescription: string;
}